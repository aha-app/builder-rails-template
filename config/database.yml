default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: builder_application_development

test:
  url: <%= ENV["TEST_DATABASE_URL"] %>

production:
  url: <%= ENV["DATABASE_URL"] %>
